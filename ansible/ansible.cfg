[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
timeout = 30
result_format = yaml

[ssh_connection]
ssh_args = -o IdentitiesOnly=yes -o PreferredAuthentications=publickey
